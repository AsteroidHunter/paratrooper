# Exact versions the Paratrooper web image runs today.
#
# Read on 2026-09-07 from the live web service's own interpreter
# (`python -m pip freeze` inside the running container, Python 3.12.14 on
# Debian 13 trixie). Nothing here is an upgrade: every line is the version
# already serving traffic.
#
# pyproject.toml keeps the intent ranges (the floors the code actually needs);
# this file is what makes a rebuild land on the same set the range happened to
# resolve to. Dockerfile.web applies it with `pip install -c`. Transitive
# packages are pinned here precisely because they cannot be pinned in
# pyproject.toml without pretending they are direct dependencies.
#
# To bump on purpose, see PINNING.md. Do not hand-edit a single line: the file
# is a reading of a container, and a lone edit makes it a guess.

aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cryptography==50.0.1
fastapi==0.141.1
frozenlist==1.8.0
h11==0.16.0
http_ece==1.2.1
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
multidict==6.7.1
numpy==2.5.3
pillow==12.3.0
propcache==0.5.2
py-vapid==1.9.4
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
python-dotenv==1.2.3
python-multipart==0.0.32
pywebpush==2.5.0
PyYAML==6.0.3
redis==8.1.0
requests==2.34.2
scipy==1.18.1
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
yarl==1.24.5
